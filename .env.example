# Set these in the Dokploy Environment tab for the compose service
DB_PASSWORD=change-me-long-random
# Random string used to hash/sign sessions, e.g. `openssl rand -base64 32`
APP_SECRET=change-me-random-base64
